const profilesReducer = (state, action) => {
  // switch (action.type) {
  //   case "GET_USERS":
  //     return {
  //       ...state,
  //       profiles: action.payload
  //     }
  //     default:
  //       return state
  // }
};

export default profilesReducer;
